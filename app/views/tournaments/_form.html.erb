<%= simple_form_for(@tournament) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :date %>
    <%= f.input :description %>
    <%= f.input :points_percentage %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
